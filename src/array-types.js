// @flow
// Arrays
let numberArray: Array < number > = [0, 1, 2, 3];
let mixedArray: Array < mixed > = [0, true, "Hello", {
  foo: "bar"
}];
let boolArray: boolean[] = [true, false]; // Shorthand syntax
// Tuples
let aTuple: [string, number, string] = ["Hi", 42, "Bye"];